{{- define "configmaps.init-container-scripts" -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: init-container-scripts
  namespace: {{ .Values.namespace }}
data:
  readiness-probe.sh: |
{{ include "configmaps.init-container-scripts.readiness-probe.sh" . | indent 4 }}
{{- end -}}
